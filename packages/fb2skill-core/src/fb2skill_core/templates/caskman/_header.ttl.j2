# =============================================================================
# {{ skill.name }}.ttl
# -----------------------------------------------------------------------------
# Capability & Skill description of the IEC 61499 "{{ skill.name }}" application,
# deployed on the {{ config.resource }} resource.
#
# Generated by fb2skill from:
#   - IEC 61499 project : {{ config.project_label }}
#   - OPC UA node set   : {{ config.source_label }}  (UAObject "{{ skill.name }}")
#
# The {{ skill.name }} function block exposes a BasicSKILL instance whose
# ISA-88 / PackML state machine is driven by writing an INT command to SKILL_CMD
# and observed by reading the INT CURRENT_STATE variable.
#
# OPC UA server : {{ config.endpoint_url }}
# Namespace ns={{ config.namespace_index }}: bound to the resource's OPC UA namespace.
#
# Modelled as a CaSkMan:OpcUaVariableSkillInterface so that an
# OpcUaVariableSkillExecutor can fire transitions by writing the command value.
#
# SKILL_CMD command values : Start=1 Reset=2 Hold=3 Unhold=4 Suspend=5
#                            Unsuspend=6 Clear=7 Stop=8 Abort=9
# CURRENT_STATE values     : Idle=1 Starting=2 Execute=3 Completing=4 Complete=5
#                            Resetting=6 Holding=7 Held=8 Unholding=9
#                            Suspending=10 Suspended=11 Unsuspending=12
#                            Stopping=13 Stopped=14 Aborting=15 Aborted=16
#                            Clearing=17
# =============================================================================
